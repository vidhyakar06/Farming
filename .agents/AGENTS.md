# Agent Rules - Farming Project

## Automatic Git Commits & Pushes
- After making code or configuration edits to files in this repository, automatically stage (`git add .`), commit (`git commit -m "..."`), and push (`git push origin main`) the changes.
- Always ensure local branch is synchronized with remote (`git pull --rebase origin main`) prior to pushing.
